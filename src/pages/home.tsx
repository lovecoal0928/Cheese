import { NextPage } from "next";
import { Home } from "@/components/templates/Home";
import React from "react";

const home:NextPage = () => {
  return <Home title={""} address={""} src={""} comment={""}/>;
};

export default home